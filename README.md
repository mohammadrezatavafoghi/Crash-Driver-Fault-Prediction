# Crash-Driver-Fault-Prediction
Predicting driver at-fault status in traffic crashes using XGBoost, Stacking (DT+XGB+SVM), and classical ML. Includes feature engineering, outlier detection (IQR/Isolation Forest), KNN imputation, and clustering (K-Means/DBSCAN). 84% test accuracy.
